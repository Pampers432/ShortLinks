ShortLinks

Сервис для сокращения URL на платформе .NET 10 с использованием Entity Framework Core и MariaDB/MySQL.

Позволяет:

Создавать короткие ссылки, которые невозможно предугадать

Редирект по коротким ссылкам с подсчетом переходов

Управлять ссылками через интерфейс: редактировать, удалять

Автоматически применять миграции при старте

Проект полностью запускается через Docker и docker-compose, достаточно указать строку подключения к локальной базе данных.

Технологии

.NET 10 LTS

ASP.NET MVC / Razor Pages

Entity Framework Core (Code First)

MariaDB / MySQL

IMemoryCache для ускорения редиректов

Docker & Docker Compose

Быстрый запуск через Docker
1. Склонировать репозиторий
git clone https://github.com/Pampers432/ShortLinks.git
cd ShortLinks
2. Настроить строку подключения

В docker-compose.yml в секции environment укажи строку подключения к локальной MariaDB:

ConnectionStrings__DefaultConnection: "server=host.docker.internal;port=3306;database=ShortLinksDb;user=root;password=1234;Pooling=true;MaximumPoolSize=50;"

3. Собрать и запустить контейнер
docker-compose up -d

Контейнер автоматически применяет миграции при старте

Приложение доступно по адресу: http://localhost:8080

4. Остановить контейнер
docker-compose down

👨‍💻 Контакты

Максим Поляков
max23012007@gmail.com
telegram @S1gmash
